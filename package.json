{
  "name": "cabalspy-wallet-tracker",
  "version": "1.0.0",
  "description": "Example: a full wallet profile page built on the CabalSpy REST API (tracker, holdings, history and PnL calendar) in TypeScript.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
